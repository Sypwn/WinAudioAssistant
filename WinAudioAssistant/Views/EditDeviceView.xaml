<Window x:Class="WinAudioAssistant.Views.EditDeviceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinAudioAssistant.Views"
        xmlns:viewmodels="clr-namespace:WinAudioAssistant.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="300" Width="300" MinWidth="300">
    <Window.DataContext>
        <viewmodels:EditDeviceViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name field -->
        <DockPanel Grid.Row="0" LastChildFill="True" ToolTip="Set a custom name for this managed device.">
            <TextBlock Text="Name:" Margin="0,0,5,0"/>
            <TextBox Text="{Binding DeviceName}" Width="Auto" />
        </DockPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5">
            <Button Content="Apply" Width="75" Margin="5,0" Click="ApplyButton_Click" ToolTip="Saves changes without closing the dialog."/>
            <Button Content="Save" Width="75" Margin="5,0" Click="SaveButton_Click" ToolTip="Saves changes and closes the dialog."/>
            <Button Content="Cancel" Width="75" Margin="5,0" Click="CancelButton_Click" ToolTip="Closes the dialog without saving changes."/>
        </StackPanel>
    </Grid>
</Window>
